const { expect } = require("chai");
const simple = require("simple-mock");
const home = require("../app/middleware/home");

// ad super test
describe("/", () => {
  it("should call firebase", () => {});

  it("should pass the results to the index page", () => {});
});
